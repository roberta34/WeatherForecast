export default function AlertCard({
                                      message
                                  }) {
    return (
        <div className="alert-card">
            {message}
        </div>
    );
}